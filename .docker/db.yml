services:
  flyway:
    image: flyway/flyway
    command: migrate -connectRetries=5
    volumes:
      - ./scripts/projecties-db/flyway/conf:/flyway/conf
      - ./scripts/projecties-db/flyway/sql:/flyway/sql
    depends_on:
      - postgres
    networks:
      - brp-api-network

  pgadmin:
    container_name: pgadmin
    image: dpage/pgadmin4:latest
    restart: always
    environment:
      - PGADMIN_DEFAULT_EMAIL=admin@haalcentraal.nl
      - PGADMIN_DEFAULT_PASSWORD=root
    ports:
      - "5433:80"
    security_opt:
      - no-new-privileges:true
    networks:
      - brp-api-network
      
  postgres:
    container_name: db
    image: postgres:17.7-alpine
    restart: always
    environment:
      - POSTGRES_USER=root
      - POSTGRES_PASSWORD=root
    volumes:
      - ./scripts/db/schema.sql:/docker-entrypoint-initdb.d/schema.sql
      - ./scripts/projecties-db/init-user-db.sql:/docker-entrypoint-initdb.d/init-user-db.sql
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "pg_isready -U root"
        ]
      interval: 10s
      timeout: 5s
      retries: 12
    ports:
      - "5432:5432"
    networks:
      - brp-api-network
  
networks:
  brp-api-network:
    name: brp-api-network
