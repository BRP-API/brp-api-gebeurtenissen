openapi: 3.1.0
info:
  title: CloudEvents Specification YAML Format
  version: 1.0.2
components: 
  schemas:
    CloudEvent:
      type: object
      properties:
        type:
          $ref: '#/components/schemas/typedef'
        data:
          $ref: '#/components/schemas/datadef'

    typedef:
      description: |
        Describes the type of event related to the originating occurrence.
      type: string
      minLength: 1

    datadef:
      description: |
        The event payload.
      type:
        - object
        - string
        - number
        - array
        - boolean
        - "null"
