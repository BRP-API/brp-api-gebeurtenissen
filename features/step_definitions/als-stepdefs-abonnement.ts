import { When } from '@cucumber/cucumber';

When('de afnemer zich registreert als abonnee', function () {
});

When('een niet-geauthenticeerde gebruiker zich registreert als abonnee', function () {
});
